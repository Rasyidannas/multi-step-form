import { useReducer } from "react";

const defaultFormState = {
   plan: "",
   add: [],
   paymentTime: "monthly",
   totalPrice: 0
}

const FormProvider = () => {
   const [] = useReducer()

   const storeItemHandler = () => {}


}

export default FormProvider;